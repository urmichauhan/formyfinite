PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/formyfinite
APP_ORIGIN=http://localhost:3000
NODE_ENV=development
# A 64-character hex key; generate with node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DATA_ENCRYPTION_KEY=
# Optional initial admin email. Register this account before enabling public access.
ADMIN_EMAIL=
