import { CapsletterDirective } from './capsletter.directive';

describe('CapsletterDirective', () => {
  it('should create an instance', () => {
    // const directive = new CapsletterDirective();
    // expect(directive).toBeTruthy();
  });
});
