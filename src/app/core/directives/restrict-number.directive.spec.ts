import { RestrictNumberDirective } from './restrict-number.directive';

describe('RestrictNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictNumberDirective();
    expect(directive).toBeTruthy();
  });
});
